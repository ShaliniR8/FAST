<% headers = ['Sort ID', 'Title','Revision Level','Revision Date','Department','Requirement','Reference Number', 'Reference','Instructions','Created By'] %><%= CSV.generate_line headers %><%@inspection.items.each do |i|%><%= CSV.generate_line([i.sort_id,i.title,i.revision_level,i.get_revision_date,i.department,i.requirement,i.reference_number,i.reference,i.instructions,i.created_by]).html_safe%><%end%>