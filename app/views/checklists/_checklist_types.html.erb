<%=
  case CONFIG::SR::GENERAL[:checklist_version]
  when "1"
    render "/audits/items", :owner => owner, :type => owner.class.name.downcase
  when "2"
    render "/checklist_templates/show_checklist_records", :owner => owner
  when "3"
    render "/checklists/show", :owner => owner
  end
%>
