<%=render '/forms/show_fields', :fields => Record.get_meta_fields('show'), :record => owner%>

<%=render "/records/show_template_fields", :record => owner%>

<%=render 'shared/choose_show_matrix',
            :owner => owner,
            :show_btn => true,
            :type => 'reports',
            :allow_mitigate => true,
            :can_change => false%>

<%=render '/panels/show_panels', :owner => owner%>

<%=render "/forms/show_comment", :owner => owner%>
