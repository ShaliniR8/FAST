<div class="col-xs-12 col-sm-3">
  <div class="panel panel-purple">
    <div class="panel-heading">
      <div class="panel-title">
        <b>Newsletters Statistics</b>
      </div>
    </div>
    <div class="panel-body" style="height:300px">
      <div id="newsletters" style="width: 100%"></div>
      <div id="newsletters_download" style="width: 100%" align="right"></div>
    </div>
  </div>
</div>
<script type="text/javascript">

  <%= render partial: "analytics_data.js.erb", locals: {type: :newsletter} %>
  google.charts.load('current', {packages: ['corechart', 'bar']});
  google.charts.setOnLoadCallback(function() {drawChartNews(newsletterData, params);});
  function drawChartNews(data, params) {
    var letter_options = {
      chart: {title: 'Safety Promotion Statistics'},
      legend:'none',
      height:230,
      colors:['blue'],
      bars: 'horizontal' ,
      chartArea: {left:100}// Required for Material Bar Charts.
    };

    var newsletter_chart = new google.visualization.BarChart(document.getElementById('newsletters'));
    google.visualization.events.addListener(newsletter_chart, 'onmouseover', cursor_pointer);
    google.visualization.events.addListener(newsletter_chart, 'onmouseout', cursor_default);
    google.visualization.events.addListener(newsletter_chart, 'ready', function () {
      document.getElementById('newsletters_download').innerHTML = '<a href="' + newsletter_chart.getImageURI() + '"download><u>Download Chart</u></a>';
    });
    newsletter_chart.draw(google.visualization.arrayToDataTable(data), letter_options);
    // google.visualization.events.addListener(newsletter_chart, 'select', function(){
    //   var all_status=<%=raw @grouped_newsletters.keys%>
    //   var root_url="<%=newsletters_path%>?status="
    //   var selection = newsletter_chart.getSelection();
    //   var message = "";
    //   var dateParams = params ? "&start_date=" + params.startDate + "&end_date=" + params.endDate : "";
    //   for (var i = 0; i < selection.length; i++) {
    //     var item = selection[i];
    //     message= root_url+all_status[item.row] + dateParams;
    //   }
    //   window.location=message;
    // });
  }
  $(window).bind('resizeEnd', function() {
    drawChartNews(newsletterData, params);
  });
</script>
