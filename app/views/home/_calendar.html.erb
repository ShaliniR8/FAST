<%= stylesheet_link_tag "full_calendar/fullcalendar.css", :media=>"all" %>
<%=
  javascript_include_tag(
    'utility/moment.min.js',
    'utility/fullcalendar.min.js',
    'utility/collapse-panel2'
  )
%>


<div class="col-xs-12 col-lg-12">
  <div class="panel panel-primary">
    <div class="panel-heading click-heading">
      <div class="panel-title">
        <b>Calendar</b>
        <span class="pull-right clickable">
          <i class="mt5 glyphicon glyphicon-chevron-up"></i>
        </span>
      </div>
    </div>
    <div class="panel-body">
      <div id="calendar"></div>
    </div>
  </div>
</div>

<style>
  #calendar {
    margin: 20px 10px;
    font-family: "Lucida Grande",Helvetica,Arial,Verdana,sans-serif;
    font-size: 15px;
  }
  .fc-button-group,
  .fc button {
    display: block;
  }
</style>


<script>
  $(document).ready(function() {
    $('#calendar').fullCalendar({
      header: {
        left: 'prevYear,prev',
        center: 'title',
        right: "today,month,basicWeek,listWeek, next,nextYear"
      },
      height: 600,
      defaultDate: '<%=Time.now.to_date%>',
      timezone: "local",
      editable: false,
      events: <%=raw(@calendar_entries.to_json)%>,
      eventRender: function(event, element) {
        $(element).tooltip({
          title: (event.description!=undefined) ? event.description : event.title,
          container: 'body'
        });
      }
    });
  });
</script>





