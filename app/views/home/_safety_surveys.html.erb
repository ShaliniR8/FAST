<div class="col-xs-12 col-sm-3">
  <div class="panel panel-purple">
    <div class="panel-heading">
      <div class="panel-title">
        <b>Safety Surveys Statistics</b>
      </div>
    </div>
    <div class="panel-body" style="height:300px">
      <div id="safety_surveys" style="width: 100%"></div>
      <div id="safety_surveys_download" style="width: 100%" align="right"></div>
    </div>
  </div>
</div>
<script type="text/javascript">

  <%= render partial: "analytics_data.js.erb", locals: {type: :safety_survey} %>
  google.charts.load('current', {packages: ['corechart', 'bar']});
  google.charts.setOnLoadCallback(function() {drawChartSurvey(safetySurveyData, params);});
  function drawChartSurvey(data, params) {
    var survey_options = {
      chart: {title: 'Safety Promotion Statistics'},
      legend:'none',
      height:230,
      colors:['blue'],
      bars: 'horizontal' ,
      chartArea: {left:100}// Required for Material Bar Charts.
    };

    var safety_survey_chart = new google.visualization.BarChart(document.getElementById('safety_surveys'));
    google.visualization.events.addListener(safety_survey_chart, 'onmouseover', cursor_pointer);
    google.visualization.events.addListener(safety_survey_chart, 'onmouseout', cursor_default);
    google.visualization.events.addListener(safety_survey_chart, 'ready', function () {
      document.getElementById('safety_surveys_download').innerHTML = '<a href="' + safety_survey_chart.getImageURI() + '"download><u>Download Chart</u></a>';
    });
    safety_survey_chart.draw(google.visualization.arrayToDataTable(data), survey_options);
    // google.visualization.events.addListener(safety_survey_chart, 'select', function(){
    //   var all_status=<%=raw @grouped_safety_surveys.keys%>
    //   var root_url="<%=safety_surveys_path%>?status="
    //   var selection = safety_survey_chart.getSelection();
    //   var message = "";
    //   var dateParams = params ? "&start_date=" + params.startDate + "&end_date=" + params.endDate : "";
    //   for (var i = 0; i < selection.length; i++) {
    //     var item = selection[i];
    //     message= root_url+all_status[item.row] + dateParams;
    //   }
    //   window.location=message;
    // });
  }
  $(window).bind('resizeEnd', function() {
    drawChartSurvey(safetySurveyData, params);
  });
</script>
