<div class="panel panel-info">
  <div class="panel-heading">
    <div class="panel-title">Agendas
    <button type="button" class="close pull-right" data-dismiss="modal" aria-label="Close"><span class="glyphicon glyphicon-remove"></span></button></div>
  </div>
  <div class="panel-body" style="max-height: calc(100vh - 210px);overflow-y:auto">
    <div class="row">
      <%=form_for @meeting.becomes(SmsMeeting) do |f|%>

        <div class="col-xs-12" style="max-height:400px;overflow-y:auto">
          <table class="table table-hover">
            <thead>
              <tr>
                <%@headers.each do |h|%>
                    <th><%=h[:title]%></th>
                <%end%>
              </tr>
            </thead>
            <tbody id="agendas">
              <%@package.sms_agendas.each do |a|%>
                <%if a.user_id==current_user.id%>
                  <%=f.fields_for :sms_agendas,a do |x|%>
                    <tr>
                      <td><%=x.text_field :title,:class=>"form-control"%></td>
                      <td><%=x.select :status,options_for_select(@status,a.status),{:include_blank=>""},:class=>"form-control"%></td>
                      <td><%=a.get_user%></td>
                      <td><%=a.get_created%></td>
                      <td><%=a.get_updated%></td>
                      <td><%=x.select :discussion,options_for_select(@tof,a.discussion),{:include_blank=>""},:class=>"form-control"%></td>
                      <td><%=x.select :accepted,options_for_select(@accept_deline,a.accepted),{:include_blank=>""},:class=>"form-control"%></td>
                    </tr>
                    <tr>
                      <td><b>Comment</b></td>
                      <td colspan="6"><%=x.text_area :comment,:rows=>"2",:class=>"form-control"%></td>
                    </tr>
                  <%end%>
                <%else%>
                  <tr>
                    <td><%=a.title%></td>
                    <td><%=a.status%></td>
                    <td><%=a.get_user%></td>
                    <td><%=a.get_created%></td>
                    <td><%=a.get_updated%></td>
                    <td><%=a.discussion ? "Yes" : "No"%></td>
                    <td><%=a.accepted ? "Accepted" : "Declined"%></td>
                  </tr>
                  <tr>
                    <td><b>Comment</b></td>
                    <td colspan="6"><%=a.comment.gsub(/\n/, '<br/>').html_safe%></td>
                  </tr>
                <%end%>
              <%end%>
            </tbody>
          </table>
        </div>
        <div class="col-xs-12">
          <%=f.submit "Submit",:class=>"btn btn-success pull-right"%>
          <%=link_to_fields "Add",f,:sms_agendas,"SmsAgenda","agenda_fields","agendas",:class=>"btn btn-info pull-right mr5"%>
        </div>
      <%end%>
    </div>
  </div>
</div>
