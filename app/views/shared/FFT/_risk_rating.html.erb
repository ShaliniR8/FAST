<div class="col-xs-12">
  <hr>
  <h4><b>RISK RATING KEY</b></h4>
  <font size="2">
  <table class="table table-bordered table-fixed table-left">
    <tr>
      <%FFT_Config::MATRIX_INFO[:risk_definitions].each do |k, v| %>
        <th bgcolor="<%=v[:color].present? ? v[:color] : k%>" width="20%">
          <h4><%=v[:rating]%></h4>
        </th>
      <%end%>
    </tr>
    <tr>
      <%FFT_Config::MATRIX_INFO[:risk_definitions].each do |k, v| %>
        <td bgcolor="<%=v[:color].present? ? v[:color] : k%>" width="20%">
          <%=v[:description]%>
        </td>
      <%end%>
    </tr>
    <tr>
      <%FFT_Config::MATRIX_INFO[:risk_definitions].each do |k, v| %>
        <td bgcolor="<%=v[:color].present? ? v[:color] : k%>" width="20%">
          <%=v[:description_approval]%>
        </td>
      <%end%>
    </tr>
  </table>
  </font>
  <p>* Risk Acceptance authority can be delegated on a temporary basis</p>
</div>

