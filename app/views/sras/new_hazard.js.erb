swal({
  title: "Add New Hazard",
  html: "<%= escape_javascript render('/sras/new_hazard')%>",
  customClass: "custom-swal",
  showCancelButton: false,
  showConfirmButton: false,
  allowOutsideClick: true,
}).catch(swal.noop);
